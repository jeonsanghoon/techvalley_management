# 로컬 env — source 후 export (SSOT: process-deploy.yaml)
# cp env.local.example env.local && set -a && source env.local && set +a

TV_MONGO_URI=mongodb://tv:tv_local_dev@127.0.0.1:27000/iot_service?authSource=admin&directConnection=true
TV_POSTGRES_URI=postgresql://tv:tv_local_dev@127.0.0.1:25432/iot_analytics

PGHOST=127.0.0.1
PGPORT=25432
PGUSER=tv
PGPASSWORD=tv_local_dev
PGDATABASE=iot_analytics

MONGO_URI=mongodb://tv:tv_local_dev@127.0.0.1:27000/iot_service?authSource=admin&directConnection=true

MINIO_ENDPOINT=http://127.0.0.1:19010
MINIO_CONSOLE=http://127.0.0.1:19011
MINIO_ROOT_USER=tv
MINIO_ROOT_PASSWORD=tv_local_dev
MINIO_BUCKET=tv-analytics-raw
MINIO_MEDIA_BUCKET=tv-media-upload

# Lambda S3 SDK (file-upload-orchestrator — next)
AWS_ENDPOINT_URL=http://127.0.0.1:19010
AWS_ACCESS_KEY_ID=tv
AWS_SECRET_ACCESS_KEY=tv_local_dev
AWS_REGION=ap-northeast-2
S3_FORCE_PATH_STYLE=true

# Frontend (optional)
# NEXT_PUBLIC_API_URL=http://localhost:3002/api
