"use client";

/**
 * 앱 마운트 시 light/dark 테마 리소스를 import로 선로딩.
 * MUI·Apex·AG Grid CSS 변수는 globals + data-color-mode로 전환.
 */
import "@/lib/theme-registry";

export function ThemeWarmup() {
  return null;
}
